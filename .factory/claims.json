[
  {
    "id": "cli-demo",
    "claim": "--demo copies and audits the bundled sample in an isolated temporary directory.",
    "where": "README demo section and CLI help",
    "test": "npm run test:claims -- --grep @claim:cli-demo",
    "sandbox": "release binary; shipped sample hash; unique OS temporary directory"
  },
  {
    "id": "browser-demo",
    "claim": "Try it with sample data shows a computed rootless Podman mismatch in one click on a 390 by 844 screen.",
    "where": "landing first screen",
    "test": "npm run test:claims -- --grep @claim:browser-demo",
    "sandbox": "fresh 390 by 844 browser context at home; click once; result title, mapped identity, and access branch all in viewport"
  },
  {
    "id": "permission-verdicts",
    "claim": "The CLI uses stable exit codes 0, 1, and 2 for pass, fail, and unknown verdicts.",
    "where": "README usage and CLI output",
    "test": "npm run test:claims -- --grep @claim:permission-verdicts",
    "sandbox": "three fresh numeric Docker/no-runtime fixtures"
  },
  {
    "id": "read-only-safety",
    "claim": "The audit changes no project files, starts no container, and makes at most four read-only runtime calls.",
    "where": "landing proof strip and README",
    "test": "npm run test:claims -- --grep @claim:read-only-safety",
    "sandbox": "fresh image-only project plus a recording rootless Podman adapter returning numeric image metadata and live UID/GID maps"
  },
  {
    "id": "config-support",
    "claim": "The CLI reads JSONC Dev Container files and selected Compose service metadata.",
    "where": "landing how-it-works section and README",
    "test": "npm run test:claims -- --grep @claim:config-support",
    "sandbox": "fresh JSONC and Compose sample with a bind mount"
  },
  {
    "id": "compose-user-precedence",
    "claim": "For a selected Compose service, its user overrides containerUser while remoteUser overrides that service user.",
    "where": "README identity precedence",
    "test": "npm run test:claims -- --grep @claim:compose-user-precedence",
    "sandbox": "fresh packed and installed CLI; both root/non-root conflict directions at both precedence levels on root-owned mode 0755 workspaces"
  },
  {
    "id": "share-redaction",
    "claim": "--share removes project, workspace, configuration, and runtime-wrapper paths from JSON reports.",
    "where": "README and Privacy",
    "test": "npm run test:claims -- --grep @claim:share-redaction",
    "sandbox": "fresh project with private path segments and a missing runtime wrapper"
  },
  {
    "id": "report-contract",
    "claim": "JSON reports use schema version 1 and retain neutral path labels in share mode.",
    "where": "README usage",
    "test": "npm run test:claims -- --grep @claim:report-contract",
    "sandbox": "fresh passing Docker/no-runtime project"
  },
  {
    "id": "runtime-mapping",
    "claim": "The report identifies Docker direct IDs and rootless Podman live UID/GID maps as separate runtime cases.",
    "where": "landing runtime table and README",
    "test": "npm run test:claims -- --grep @claim:runtime-mapping",
    "sandbox": "direct Docker fixture and recording rootless Podman adapter"
  },
  {
    "id": "docker-userns-remap",
    "claim": "Docker userns-remap returns UNKNOWN unless its host UID/GID map is resolved.",
    "where": "landing runtime table and README supported inputs and limits",
    "test": "npm run test:claims -- --grep @claim:docker-userns-remap",
    "sandbox": "fresh packed and installed CLI; mode-0755 workspace owned 1000:1000; Docker info adapter reporting name=userns"
  },
  {
    "id": "read-only-remediation",
    "claim": "Read-only mount failures recommend reviewing readonly, read_only, or ro instead of changing identity or host mode.",
    "where": "README supported inputs and limits",
    "test": "npm run test:claims -- --grep @claim:read-only-remediation",
    "sandbox": "fresh packed and installed CLI; matching workspace identity; three workspaceMount read-only spellings"
  },
  {
    "id": "conservative-identities",
    "claim": "Named, UID-only, build-backed, and reserved Linux identities return UNKNOWN until a numeric UID:GID is proven.",
    "where": "README limits and browser validation",
    "test": "npm run test:claims -- --grep @claim:conservative-identities",
    "sandbox": "four fresh CLI fixtures and direct browser validation"
  },
  {
    "id": "browser-private",
    "claim": "The browser sample sends no project data and stores no entered values.",
    "where": "landing first screen, browser sample, Privacy, README",
    "test": "npm run test:claims -- --grep @claim:browser-private",
    "sandbox": "fresh browser context with request, cookie, Web Storage, and IndexedDB capture"
  },
  {
    "id": "cli-private",
    "claim": "The CLI includes no network or telemetry client and requires no account.",
    "where": "README privacy section",
    "test": "npm run test:claims -- --grep @claim:cli-private",
    "sandbox": "normal dependency manifest, Rust source scan, and bundled demo execution"
  },
  {
    "id": "offline-reload",
    "claim": "The browser sample works offline after the first visit.",
    "where": "offline banner, README, demo documentation",
    "test": "npm run test:claims -- --grep @claim:offline-reload",
    "sandbox": "dedicated fresh context set offline after service-worker activation"
  },
  {
    "id": "browser-parity",
    "claim": "The browser sample follows the CLI owner, group, mode, and rootless mapping rules.",
    "where": "landing browser sample",
    "test": "npm run test:claims -- --grep @claim:browser-parity",
    "sandbox": "identical browser and packed-CLI outer ranges; container IDs below, equal to, and above kept 1000, including root; safe recovery"
  },
  {
    "id": "mit-license",
    "claim": "Mount Identity Audit is free under the MIT License.",
    "where": "landing first screen, README, Terms",
    "test": "npm run test:claims -- --grep @claim:mit-license",
    "sandbox": "landing copy, package metadata, and shipped license text"
  },
  {
    "id": "browser-report-details",
    "claim": "The browser sample reports the mapped host identity and the selected permission branch.",
    "where": "landing browser sample",
    "test": "npm run test:claims -- --grep @claim:browser-report-details",
    "sandbox": "fresh browser query-demo context with the bundled mismatch"
  },
  {
    "id": "config-discovery",
    "claim": "The CLI discovers .devcontainer/devcontainer.json, .devcontainer.json, and devcontainer.json in that order.",
    "where": "README usage",
    "test": "npm run test:claims -- --grep @claim:config-discovery",
    "sandbox": "fresh projects with each supported path and a precedence fixture"
  },
  {
    "id": "runtime-optional",
    "claim": "Docker or Podman is optional when every identity is supplied numerically.",
    "where": "README install",
    "test": "npm run test:claims -- --grep @claim:runtime-optional",
    "sandbox": "fresh numeric project with --no-runtime"
  },
  {
    "id": "report-limits",
    "claim": "Detailed reports list POSIX ACLs, security labels, and remote filesystem policy as limits.",
    "where": "README limits",
    "test": "npm run test:claims -- --grep @claim:report-limits",
    "sandbox": "fresh pass, fail, and unknown CLI reports"
  },
  {
    "id": "compose-build-image",
    "claim": "A Compose service with build and image returns UNKNOWN instead of trusting a stale image tag.",
    "where": "README limits",
    "test": "npm run test:claims -- --grep @claim:compose-build-image",
    "sandbox": "fresh Compose build-plus-image project and metadata-only Docker adapter"
  },
  {
    "id": "install-binary",
    "claim": "A packed crate installs one mount-identity-audit executable with help output.",
    "where": "README install",
    "test": "npm run test:claims -- --grep @claim:install-binary",
    "sandbox": "fresh cargo package, unpack, and isolated cargo-install prefix"
  },
  {
    "id": "build-artifacts",
    "claim": "The build command produces the release executable and deployable dist/site directory.",
    "where": "README develop, test, and build",
    "test": "npm run test:claims -- --grep @claim:build-artifacts",
    "sandbox": "clean build outputs from the claim-suite build step"
  }
]
